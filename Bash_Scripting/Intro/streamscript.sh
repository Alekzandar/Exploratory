#!/bin/bash

# 1 is standard output, 2 is standard error, 1 is everything that is not an error, 2 is everything that is an error 
# 2> /dev/null, a black hole, nothing that goes in comes out
# find /etc -type f 2> /dev/null, standard errors are sent into the black hole

#find /etc -type f 1>find_output.txt 2>find_errors.txt

sudo 
